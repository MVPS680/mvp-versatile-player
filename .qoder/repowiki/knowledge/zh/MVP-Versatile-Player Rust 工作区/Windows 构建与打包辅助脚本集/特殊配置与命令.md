首次使用前需运行 .\scripts\setup-ffmpeg.ps1（或加 -NoDownload 指定已有 FFmpeg 目录），它会自动下载约 1 GB 的 FFmpeg 开发包并生成 .cargo/config.toml；之后用 .\scripts\dev.ps1 <cargo args> 运行 cargo 命令，或用 .\scripts\package.ps1 [-SkipBuild] [-Output <dir>] 打包发布目录。