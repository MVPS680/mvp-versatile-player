/target
/target-sub
/target-sub-linux
/dist
*.pdb
*.ilk
*.exp
.DS_Store
Thumbs.db
# heroui-agents-md
.heroui-docs/

# 本机生成的 FFmpeg / libclang 路径（由 scripts/setup-ffmpeg.ps1 写出）。
# 它记录的是「这台机器上 SDK 装在哪」，不该跟着仓库走：入库会让每次
# git pull 都用别人的绝对路径盖掉自己的。
.cargo/config.toml

# 本机临时脚本（抓界面截图、探测像素、驱动 UI 等）。它们写死的是这台
# 机器的窗口坐标与截图路径，对别人没有意义；真正要长期保留的脚本放在
# scripts/ 下。
/tmp/

MVP-Versatile-Player2.0重构版PPT.pptx